export enum UserRole {
  Admin = "admin",
  User = "user",
  Manager = "manager",
}

export enum UserStatus {
  Active = "active",
  Inactive = "inactive",
  OnVacations = "on_vacations",
  Ill = "ill",
  Other = "other",
}
